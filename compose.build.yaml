services:
  web:
    build:
      context: .
      dockerfile: docker/Dockerfile
      target: web-runtime
  worker:
    build:
      context: .
      dockerfile: docker/Dockerfile
      target: worker-runtime
